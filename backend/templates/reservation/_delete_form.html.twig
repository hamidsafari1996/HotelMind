<form method="post" action="{{ path('app_reservation_delete', {'id': hotel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.id) }}">
    <button class="btn font-medium text-red-600 hover:text-red-700 cursor-pointer mr-3">Löschen</button>
</form>
